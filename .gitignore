.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Credentials. `.env.bak` was tracked once, with a live OctoPrint key in it, because
# only the exact name `.env` was ignored. Ignore the whole family.
.env
.env.*
*.bak
!.env.example

# Generated geometry — regenerate with vtp.cad.design()
output/*
!output/.gitkeep
